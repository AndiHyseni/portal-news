export enum NEWS {
  GET_NEWS = "News",
  GET_NEWSID = "news",
}

export enum CREATE_NEWS {
  CREATE_NEWS = "addnews",
}

export enum SAVED_NEWS {
  SAVED_NEWS = "addSavedNews",
  GET_SAVED = "getSaved",
  DELETE_SAVED = "deleteSaved",
}
